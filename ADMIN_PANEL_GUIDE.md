# 📊 Руководство по Админ-панели "Город Спорта"

## 📋 Содержание
1. [Обзор](#обзор)
2. [Архитектура](#архитектура)
3. [Установка и запуск](#установка-и-запуск)
4. [Структура проекта](#структура-проекта)
5. [API эндпоинты](#api-эндпоинты)
6. [Компоненты](#компоненты)
7. [Система верификации отзывов](#система-верификации-отзывов)
8. [Деплой](#деплой)
9. [Добавление новых функций](#добавление-новых-функций)

---

## 🎯 Обзор

Админ-панель "Город Спорта" — это веб-приложение для управления Telegram Mini App. Позволяет администраторам управлять пользователями, заданиями, призами и отслеживать активность.

**URL:** https://gsbot18.ru/admin/

**Технологии:**
- **Frontend:** React 18 + TypeScript + Vite
- **Styling:** Tailwind CSS
- **Data Fetching:** TanStack Query (React Query)
- **Routing:** React Router v6
- **Charts:** Recharts
- **Notifications:** Sonner (toast notifications)
- **Backend:** Node.js + Express + PostgreSQL

---

## 🏗️ Архитектура

```
┌─────────────────┐
│   Браузер       │
│  (Admin Panel)  │
└────────┬────────┘
         │ HTTPS
         ▼
┌─────────────────┐
│  Static Files   │
│  /deploy-admin/ │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Backend API    │
│  /admin/api/*   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   PostgreSQL    │
│   Database      │
└─────────────────┘
```

### Потоки данных:

1. **Аутентификация:** Пароль → Backend → JWT токен → LocalStorage
2. **Запросы:** Frontend → API с Bearer токеном → Database → Response
3. **Деплой:** Local → GitHub → Server (git pull) → Nginx serves files

---

## 🚀 Установка и запуск

### Локальная разработка

```bash
# 1. Перейти в директорию админки
cd webapp-admin

# 2. Установить зависимости
npm install

# 3. Запустить dev сервер
npm run dev

# Откроется на http://localhost:5173/admin/
```

### Сборка для продакшена

```bash
# Собрать проект (выходная директория: ../deploy-admin)
npm run build

# Предпросмотр production build
npm run preview
```

---

## 📁 Структура проекта

```
webapp-admin/
├── src/
│   ├── components/          # Переиспользуемые компоненты
│   │   ├── Layout.tsx       # Основной лейаут с навигацией
│   │   ├── EditUserDialog.tsx    # Диалог редактирования пользователя
│   │   ├── EditTaskDialog.tsx    # Диалог редактирования задания
│   │   └── EditPrizeDialog.tsx   # Диалог редактирования приза
│   │
│   ├── pages/               # Страницы приложения
│   │   ├── Login.tsx        # Страница входа
│   │   ├── Dashboard.tsx    # Главная (статистика + графики)
│   │   ├── Users.tsx        # Список пользователей + поиск
│   │   ├── UserDetails.tsx  # Детальная страница пользователя
│   │   ├── Tasks.tsx        # Управление заданиями
│   │   ├── Prizes.tsx       # Управление призами
│   │   ├── Purchases.tsx    # История покупок
│   │   ├── Referrals.tsx    # Таблица рефералов для отдела продаж
│   │   └── Reviews.tsx      # Проверка скриншотов отзывов на Яндекс.Картах
│   │
│   ├── lib/
│   │   └── api.ts           # API клиент (все запросы к backend)
│   │
│   ├── App.tsx              # Роутинг и главный компонент
│   ├── main.tsx             # Точка входа
│   └── index.css            # Tailwind импорты
│
├── public/                  # Статические файлы
├── index.html               # HTML шаблон
├── vite.config.ts           # Конфигурация Vite
├── tailwind.config.js       # Конфигурация Tailwind
├── tsconfig.json            # TypeScript конфиг
└── package.json             # Зависимости и скрипты
```

---

## 🔌 API эндпоинты

Все эндпоинты находятся в `bot/index.js` на сервере.

### Аутентификация

```http
POST /admin/api/login
Content-Type: application/json

{
  "password": "your_password"
}

Response:
{
  "success": true,
  "token": "jwt_token_here"
}
```

### Статистика

```http
GET /admin/api/stats
Authorization: Bearer {token}

Response:
{
  "totalUsers": 100,
  "activeUsers": 50,
  "totalCoins": 5000,
  "totalTasks": 30,
  "completedTasks": 200
}
```

```http
GET /admin/api/stats/charts
Authorization: Bearer {token}

Response:
{
  "usersByDay": [...],
  "tasksByDay": [...],
  "coinsByDay": [...]
}
```

### Пользователи

```http
# Список всех пользователей
GET /admin/api/users
Authorization: Bearer {token}

Response: Array<User>
```

```http
# Детали пользователя + задания + покупки
GET /admin/api/users/:id
Authorization: Bearer {token}

Response:
{
  "user": {...},
  "tasks": [...],
  "purchases": [...]
}
```

```http
# Задания пользователя
GET /admin/api/users/:id/tasks
Authorization: Bearer {token}

Response: Array<UserTask>
```

```http
# Покупки пользователя
GET /admin/api/users/:id/purchases
Authorization: Bearer {token}

Response: Array<Purchase>
```

```http
# Обновить монеты/XP пользователя
POST /admin/api/users/:id/update
Authorization: Bearer {token}
Content-Type: application/json

{
  "coins": 100,      // Добавить/отнять монеты
  "xp": 50,          // Добавить/отнять XP
  "reason": "Награда за активность"
}
```

```http
# Удалить пользователя
DELETE /admin/api/users/:id
Authorization: Bearer {token}

Response:
{
  "success": true,
  "message": "Пользователь удалён"
}

Каскадное удаление (в таком порядке):
1. Все выполненные задания (user_tasks)
2. Все покупки (purchases)
3. Записи в рефералах (referrals, колонка user_id)
4. Сам пользователь (users)
```

### Задания

```http
# Список всех заданий
GET /admin/api/tasks
Authorization: Bearer {token}

Response: Array<Task>
```

```http
# Обновить задание
PUT /admin/api/tasks/:id
Authorization: Bearer {token}
Content-Type: application/json

{
  "title": "Новое название",
  "description": "Описание",
  "coins_reward": 50,
  "verification_type": "qr_code",
  "verification_data": {"code": "SPORT123"}
}

Примечание: Все 5 полей обязательны (включая verification_type и verification_data)
```

### Призы

```http
# Список всех призов
GET /admin/api/prizes
Authorization: Bearer {token}

Response: Array<Prize>
```

```http
# Обновить приз
PUT /admin/api/prizes/:id
Authorization: Bearer {token}
Content-Type: application/json

{
  "title": "Название приза",
  "description": "Описание",
  "price": 500,
  "icon": "🎁",
  "is_active": true
}
```

### Покупки

```http
# Список всех покупок
GET /admin/api/purchases
Authorization: Bearer {token}

Response: Array<Purchase>
```

### Рефералы

```http
# Список всех рефералов (для отдела продаж)
GET /admin/api/referrals
Authorization: Bearer {token}

Response: Array<{
  id: number,
  user_id: number,
  friend_name: string,
  friend_phone: string,
  created_at: string,
  first_name: string,
  last_name: string,
  phone: string
}>
```

### Управление заданиями пользователя

```http
# Засчитать или отменить задание пользователю (только admin)
POST /admin/api/users/:id/toggle-task
Authorization: Bearer {token}
Content-Type: application/json

{
  "taskId": 5,
  "action": "complete"   // "complete" | "uncomplete"
}

Response (complete):
{
  "success": true,
  "message": "Задание засчитано",
  "coins_change": 50,
  "xp_change": 25
}

Response (uncomplete):
{
  "success": true,
  "message": "Выполнение задания отменено",
  "coins_change": -50,
  "xp_change": -25
}

Примечания:
- action="complete": добавляет запись в user_tasks, начисляет coins и xp
- action="uncomplete": удаляет запись из user_tasks, списывает coins и xp
- Баланс не уходит в минус (используется GREATEST(0, coins - reward))
- Только администратор (role=admin) имеет доступ
```

### Отзывы на Яндекс.Картах

```http
# Список заявок на проверку отзывов
GET /admin/api/reviews?status=pending
Authorization: Bearer {token}

# status: pending | approved | rejected | all (по умолчанию pending)

Response: Array<{
  id: number,
  user_id: number,
  task_id: number,
  photo_file_id: string,
  photo_url: string,        # путь вида /uploads/reviews/filename.jpg
  status: string,
  admin_comment: string,
  submitted_at: string,
  reviewed_at: string,
  reviewed_by: string,
  first_name: string,
  last_name: string,
  username: string,
  task_title: string,
  coins_reward: number
}>
```

```http
# Счётчик pending заявок (для бейджа в меню)
GET /admin/api/reviews/count
Authorization: Bearer {token}

Response:
{
  "count": 3
}
```

```http
# Одобрить отзыв (начисляет монеты пользователю)
POST /admin/api/reviews/:id/approve
Authorization: Bearer {token}

Response:
{
  "success": true,
  "message": "Отзыв одобрен"
}

Действия при одобрении:
1. Статус заявки → "approved"
2. Засчитывается задание (user_tasks)
3. Начисляются монеты и XP пользователю
4. Telegram уведомление пользователю: "Ваш отзыв одобрен! +N 🪙"
```

```http
# Отклонить отзыв (с комментарием)
POST /admin/api/reviews/:id/reject
Authorization: Bearer {token}
Content-Type: application/json

{
  "comment": "Причина отклонения"
}

Response:
{
  "success": true,
  "message": "Отзыв отклонён"
}

Действия при отклонении:
1. Статус заявки → "rejected"
2. admin_comment сохраняется
3. Telegram уведомление пользователю: "Отзыв не принят. Причина: ..."
```

```http
# Загрузка скриншота из мини-приложения
POST /api/upload-review
Content-Type: multipart/form-data

Fields:
  photo      - файл изображения (до 10MB, только image/*)
  telegramId - telegram_id пользователя
  taskId     - day_number задания

Response:
{
  "success": true,
  "message": "Скриншот отправлен на проверку"
}

Ошибки:
  { "error": "No file uploaded" }         - файл не прикреплён
  { "error": "User not found" }           - пользователь не найден
  { "error": "Task not found" }           - задание не найдено
  { "error": "Заявка уже подана" }        - уже есть pending/approved заявка
```

---

## 🧩 Компоненты

### Layout.tsx
**Описание:** Основной лейаут с боковой навигацией

**Функции:**
- Рендерит навигационное меню
- Проверяет аутентификацию
- Редирект на /login если нет токена

### EditUserDialog.tsx
**Описание:** Модальное окно для изменения монет/XP пользователя

**Пропсы:**
```typescript
{
  user: User,
  isOpen: boolean,
  onClose: () => void
}
```

**Функции:**
- Добавить/отнять монеты
- Добавить/отнять XP
- Указать причину изменения
- **Автозаполнение текущих значений** при открытии диалога
- **Показ дельты изменений** (зеленым/красным)
- **Предпросмотр изменений** до сохранения
- Toast уведомления об успехе/ошибке

### EditTaskDialog.tsx
**Описание:** Модальное окно для редактирования задания

**Пропсы:**
```typescript
{
  task: Task,
  isOpen: boolean,
  onClose: () => void
}
```

**Функции:**
- Изменить название, описание
- Изменить награды (монеты)
- **Редактирование проверочных кодов** (показываются для типов `qr`, `app_code`, `qr_or_manual`):
  - 🔧 Тестовый код (test_code) - синий, для внутренней проверки
  - 📱 QR-код (qr_code) - зелёный, длинный код для QR
  - ⌨️ Ручной код (manual_code) - фиолетовый, только для типа `qr_or_manual`
- **Порядок проверки на бэкенде:** test_code → manual_code → qr_code
- **Все три кода работают** независимо и принимаются без учёта регистра
- **Автозаполнение текущих значений** при открытии
- **Показ изменений** до сохранения
- Коды работают независимо - можно заполнить 1, 2 или все 3

### EditPrizeDialog.tsx
**Описание:** Модальное окно для редактирования приза

**Пропсы:**
```typescript
{
  prize: Prize,
  isOpen: boolean,
  onClose: () => void
}
```

**Функции:**
- Изменить название, описание, цену
- Активировать/деактивировать приз

### Users.tsx
**Описание:** Страница со списком пользователей

**Функции:**
- Поиск по имени, username, телефону, telegram_id
- Таблица с данными пользователей
- Кнопка "Подробнее" (редактирование доступно внутри детальной страницы)
- **Excel экспорт** с полными данными:
  - Основные данные (ID, имя, телефон, абонемент)
  - Метрики (спорткоины, XP, уровень, выполненные задания)
  - Данные анкеты (полное имя, дата рождения, цели, есть дети)
  - Даты (регистрация, последняя активность)
  - Корректная обработка массивов (goals) и строковых значений (hasKids)

### UserDetails.tsx
**Описание:** Детальная страница пользователя

**Функции:**
- Полная информация о пользователе
- **Блок "Данные анкеты"** с информацией из первого задания:
  - Полное имя
  - Дата рождения
  - Цели
  - Есть ли дети
- Карточки с метриками (спорткоины, XP, задания)
- **Блок "Задания"** — показывает ВСЕ задания (выполненные и нет):
  - Счётчик "Выполнено X из Y" в заголовке
  - ✓ Кнопка "Засчитать" для невыполненных заданий
  - ↩ Кнопка "Отменить" для выполненных заданий
  - Невыполненные задания затемнены (opacity-60)
  - Кнопки доступны только администратору
  - После действия — toast с количеством начисленных/списанных монет
- История покупок
- Кнопка редактирования (только admin)
- Кнопка удаления пользователя (с двойным подтверждением, только admin)
  - Первый клик: показывает предупреждение
  - Второй клик (в течение 5 секунд): удаляет пользователя
  - Каскадное удаление всех связанных данных (задания, покупки)

### Reviews.tsx
**Описание:** Страница проверки скриншотов отзывов на Яндекс.Картах

**Функции:**
- Фильтры по статусу: "Ожидают" / "Одобрены" / "Отклонены" / "Все"
- Карточки с:
  - Превью скриншота (клик → lightbox с полным размером)
  - Данные пользователя (имя, @username)
  - Название задания и сумма награды
  - Дата подачи
- Кнопка **"✅ Одобрить"** — одобряет и начисляет монеты
- Кнопка **"❌ Отклонить"** — открывает форму ввода причины
- Счётчик "X ожидают проверки" в заголовке
- Автообновление каждые 30 секунд
- Бейдж с количеством в боковом меню (оранжевый, обновляется раз в минуту)

### Dashboard.tsx
**Описание:** Главная страница с аналитикой

**Функции:**
- Карточки со статистикой (пользователи, задания, монеты)
- Графики активности (Recharts)
- Обновление данных в реальном времени

---

## 📸 Система верификации отзывов

Система позволяет пользователям подтверждать выполнение задания "Оставить отзыв на Яндекс.Картах" через загрузку скриншота прямо из мини-приложения.

> ⚠️ **Важно:** Бот (@gsvtk_bot) занят Senler'ом (webhook для рассылок). Поэтому получение фото через бота невозможно — используется прямая загрузка файла через мини-приложение (multer).

### Схема работы

```
Пользователь (мини-приложение)          Сервер                   Админка
    │                                      │                         │
    ├─ Нажимает "📎 Выбрать скриншот"      │                         │
    ├─ Выбирает файл из галереи            │                         │
    ├─── POST /api/upload-review ─────────►│                         │
    │    (FormData: photo, telegramId,     │                         │
    │     taskId)                          │                         │
    │                                      ├─ Сохраняет файл в       │
    │                                      │  /uploads/reviews/      │
    │                                      ├─ INSERT review_submissions
    │◄── { success: true } ────────────────┤                         │
    │    (задание → "⏳ На проверке")       │                         │
    │                                      │◄── GET /admin/api/reviews
    │                                      │                         │
    │                                      │◄── POST .../approve     │
    │◄── Telegram: "Отзыв принят! +N 🪙" ──┤─────────────────────────┤
```

### Установка (первый раз)

#### 1. Создать папку для хранения фото

```bash
# На сервере
mkdir -p /var/www/gorodsporta/uploads/reviews
chown -R www-data:www-data /var/www/gorodsporta/uploads
```

#### 2. Создать таблицу в БД

```sql
CREATE TABLE IF NOT EXISTS review_submissions (
    id SERIAL PRIMARY KEY,
    user_id INTEGER NOT NULL REFERENCES users(id),
    task_id INTEGER NOT NULL REFERENCES tasks(id),
    photo_file_id TEXT NOT NULL,       -- имя файла на сервере
    photo_url TEXT NOT NULL,           -- путь: /uploads/reviews/filename.jpg
    status TEXT NOT NULL DEFAULT 'pending', -- pending | approved | rejected
    admin_comment TEXT,
    submitted_at TIMESTAMP NOT NULL DEFAULT NOW(),
    reviewed_at TIMESTAMP,
    reviewed_by TEXT
);

CREATE INDEX IF NOT EXISTS idx_review_submissions_user_id ON review_submissions(user_id);
CREATE INDEX IF NOT EXISTS idx_review_submissions_status ON review_submissions(status);
```

#### 3. Настроить Nginx для раздачи загруженных файлов

Добавить в конфиг `/etc/nginx/sites-enabled/gorodsporta`:

```nginx
location /uploads/ {
    alias /var/www/gorodsporta/uploads/;
    expires 30d;
    add_header Cache-Control "public";
}
```

Затем:
```bash
nginx -t && systemctl reload nginx
```

#### 4. Установить multer и перезапустить

```bash
cd /var/www/gorodsporta/bot && npm install multer
pm2 restart gorodsporta-bot
```

### Технические детали

- **Загрузка:** `POST /api/upload-review` принимает `multipart/form-data` через `multer`
- **Поля запроса:** `photo` (файл), `telegramId` (строка), `taskId` (строка)
- **Хранение фото:** `/var/www/gorodsporta/uploads/reviews/{timestamp}_{originalname}`
- **URL в БД:** `/uploads/reviews/{timestamp}_{originalname}`
- **Просмотр в админке:** `https://gsbot18.ru/uploads/reviews/...`
- **Повторная отправка:** разрешена после отклонения (`rejected`), заблокирована при `pending` или `approved`
- **Статус в мини-приложении:** `reviewPending` берётся из API (`/api/user`), не хранится локально
- **Уведомления:** бот отправляет сообщение в Telegram при одобрении/отклонении через `bot.api.sendMessage`

### SQL запросы для мониторинга

```sql
-- Все pending заявки
SELECT rs.id, u.first_name, t.title, rs.submitted_at
FROM review_submissions rs
JOIN users u ON u.id = rs.user_id
JOIN tasks t ON t.id = rs.task_id
WHERE rs.status = 'pending'
ORDER BY rs.submitted_at DESC;

-- Статистика по заявкам
SELECT status, COUNT(*) FROM review_submissions GROUP BY status;
```

---

## 🚢 Деплой

### Workflow

```bash
# 1. Разработка локально
cd webapp-admin
npm run dev

# 2. Сборка проекта
npm run build
# → Создаются файлы в /deploy-admin/

# 3. Коммит в Git
git add .
git commit -m "Update admin panel"
git push

# 4. На сервере (через консоль Timeweb или SSH)
cd /var/www/gorodsporta
git pull

# 5. Если изменения в backend - перезапустить PM2
pm2 restart gorodsporta-bot

# 6. Готово! Админка доступна на https://gsbot18.ru/admin/
```

### Деплой webapp-react (мини-приложение)

⚠️ **Важно: две папки — dist/ и deploy/**

Билд собирается в `webapp-react/dist/`, но Nginx раздаёт из `deploy/` (корень проекта). Это разные папки!

```bash
# 1. Внести изменения в webapp-react/src/...

# 2. Собрать билд
cd webapp-react
npm run build
cd ..

# 3. Скопировать в папку деплоя
cp -r webapp-react/dist/. deploy/

# 4. Закоммитить и запушить
git add webapp-react/ deploy/
git commit -m "Update webapp-react"
git push

# 5. На сервере
git pull
# pm2 restart НЕ нужен — это статические файлы
```

Если после деплоя изменения не видны — скорее всего, папка `deploy/` не была обновлена.

### Nginx конфигурация

Админка обслуживается как статические файлы:

```nginx
location /admin {
    alias /var/www/gorodsporta/deploy-admin;
    try_files $uri $uri/ /admin/index.html;
}
```

---

## 🔧 Добавление новых функций

### Пример: Добавить новую страницу "Отчеты"

#### 1. Создать страницу

```typescript
// webapp-admin/src/pages/Reports.tsx
import { useQuery } from '@tanstack/react-query';
import { api } from '@/lib/api';

export default function Reports() {
  const { data: reports, isLoading } = useQuery({
    queryKey: ['reports'],
    queryFn: () => api.getReports(),
  });

  if (isLoading) return <div>Загрузка...</div>;

  return (
    <div>
      <h1>Отчеты</h1>
      {/* Ваш UI */}
    </div>
  );
}
```

#### 2. Добавить метод в API

```typescript
// webapp-admin/src/lib/api.ts
async getReports() {
  return this.request('/admin/api/reports');
}
```

#### 3. Добавить роут

```typescript
// webapp-admin/src/App.tsx
import Reports from './pages/Reports';

<Routes>
  {/* ... другие роуты */}
  <Route path="/reports" element={<Reports />} />
</Routes>
```

#### 4. Добавить в навигацию

```typescript
// webapp-admin/src/components/Layout.tsx
const menuItems = [
  // ... существующие
  { path: '/reports', label: 'Отчеты', icon: '📊' }
];
```

#### 5. Создать backend endpoint

```javascript
// bot/index.js
app.get("/admin/api/reports", checkAdminAuth, async (req, res) => {
    try {
        const result = await pool.query(`
            SELECT * FROM reports
            ORDER BY created_at DESC
        `);
        res.json(result.rows);
    } catch (e) {
        res.status(500).json({ error: e.message });
    }
});
```

#### 6. Собрать и задеплоить

```bash
npm run build
git add .
git commit -m "Add reports page"
git push

# На сервере
git pull
pm2 restart gorodsporta-bot
```

---

## 🔐 Аутентификация

### Как работает

1. Пользователь вводит пароль на `/login`
2. Backend проверяет пароль и генерирует JWT токен
3. Токен сохраняется в `localStorage`
4. Все API запросы отправляются с заголовком: `Authorization: Bearer {token}`
5. Backend проверяет токен через middleware `checkAdminAuth`

### RBAC - Роли и доступы

В системе есть две роли:

**Администратор:**
- Пароль: `GorodSporta2025Admin!`
- Полный доступ: просмотр, редактирование, удаление
- Может менять проверочные коды заданий
- Может удалять пользователей

**Сотрудник:**
- Пароль: `GorodSporta2025Staff!`
- Только просмотр: все данные видны, но не редактируются
- Может экспортировать данные в Excel
- Не может редактировать, удалять или менять коды

### Изменить пароли

```javascript
// bot/index.js
const ADMIN_PASSWORD = "GorodSporta2025Admin!"; // Полный доступ
const STAFF_PASSWORD = "GorodSporta2025Staff!"; // Только просмотр
```

Затем перезапустить PM2:
```bash
pm2 restart all
```

---

## 🎨 Стилизация

Проект использует **Tailwind CSS**. Все стили пишутся inline через className.

### Примеры

```tsx
// Кнопка
<button className="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700">
  Нажать
</button>

// Карточка
<div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
  Контент
</div>

// Таблица
<table className="w-full">
  <thead className="bg-gray-50">
    <tr>
      <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">
        Заголовок
      </th>
    </tr>
  </thead>
</table>
```

### Кастомизация

Изменить цвета/шрифты в `tailwind.config.js`:

```javascript
module.exports = {
  theme: {
    extend: {
      colors: {
        primary: '#3B82F6',
        secondary: '#10B981',
      }
    }
  }
}
```

---

## 📊 State Management

Проект использует **TanStack Query** для управления серверным состоянием.

### Основные концепции

```typescript
// Получение данных
const { data, isLoading, error } = useQuery({
  queryKey: ['users'],
  queryFn: () => api.getUsers(),
});

// Мутация данных
const mutation = useMutation({
  mutationFn: (data) => api.updateUser(id, data),
  onSuccess: () => {
    queryClient.invalidateQueries({ queryKey: ['users'] });
    toast.success('Сохранено!');
  },
});

// Вызов мутации
mutation.mutate({ coins: 100 });
```

### Инвалидация кеша

После изменения данных нужно обновить кеш:

```typescript
queryClient.invalidateQueries({ queryKey: ['users'] });
```

---

## 🐛 Отладка

### Проверить логи backend

```bash
# На сервере
pm2 logs gorodsporta-bot

# Последние 50 строк
pm2 logs gorodsporta-bot --lines 50
```

### Проверить ошибки frontend

1. Открыть DevTools (F12)
2. Вкладка **Console** — ошибки JavaScript
3. Вкладка **Network** — запросы к API
4. Найти красный запрос → вкладка **Response** — текст ошибки

### Типичные ошибки

**401 Unauthorized:**
- Токен истек или невалиден
- Решение: перелогиниться

**500 Internal Server Error:**
- Ошибка на backend (SQL, логика)
- Решение: проверить `pm2 logs`

**404 Not Found:**
- Эндпоинт не существует
- Решение: проверить URL в `api.ts` и роуты в `bot/index.js`

---

## 📝 Toast уведомления

Проект использует библиотеку **Sonner** для красивых уведомлений.

### Использование

```typescript
import { toast } from 'sonner';

// Успех
toast.success('Пользователь обновлён!');

// Ошибка
toast.error('Не удалось сохранить');

// Предупреждение
toast.warning('Заполните все поля');

// Информация
toast.info('Изменения сохранены');
```

### Позиция

Настраивается в `App.tsx`:

```tsx
<Toaster position="top-right" richColors />
```

---

## 🗄️ База данных

### Основные таблицы

**users** — пользователи
- id, telegram_id, first_name, last_name, username
- phone, membership_type
- coins, xp, level, streak_days
- created_at, last_activity_at

**tasks** — задания
- id, day_number, title, description
- coins_reward, xp_reward
- is_active

**user_tasks** — выполненные задания
- id, user_id, task_id
- status, completed_at, verified_by

**shop_items** — призы
- id, title, description, price, icon
- is_active

**purchases** — покупки
- id, user_id, item_id
- price, status, created_at

**review_submissions** — заявки на проверку отзывов *(добавлено в v1.6)*
- id, user_id, task_id
- photo_file_id (file_id от Telegram), photo_url (путь на сервере)
- status: `pending` | `approved` | `rejected`
- admin_comment (причина отклонения)
- submitted_at, reviewed_at, reviewed_by

### Подключение к БД

```bash
psql -h localhost -U gsadmin -d gorodsporta
# Пароль: GorodSporta2025!
```

### Полезные SQL запросы

```sql
-- Посмотреть всех пользователей
SELECT * FROM users ORDER BY created_at DESC LIMIT 10;

-- Статистика по заданиям
SELECT task_id, COUNT(*) as completed
FROM user_tasks
WHERE status = 'completed'
GROUP BY task_id;

-- Топ пользователей по монетам
SELECT first_name, last_name, coins, xp
FROM users
ORDER BY coins DESC
LIMIT 10;
```

---

## 🔄 Обновление зависимостей

```bash
# Проверить устаревшие пакеты
npm outdated

# Обновить все (осторожно!)
npm update

# Обновить конкретный пакет
npm install react@latest
```

---

## 📞 Поддержка

При возникновении вопросов:

1. Проверить логи: `pm2 logs gorodsporta-bot`
2. Проверить DevTools в браузере
3. Проверить документацию используемых библиотек
4. Связаться с разработчиком

---

## 📚 Полезные ссылки

- [React Docs](https://react.dev/)
- [TanStack Query](https://tanstack.com/query/latest)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [Vite](https://vitejs.dev/)
- [Recharts](https://recharts.org/)
- [React Router](https://reactrouter.com/)

---

**Версия документа:** 1.8
**Последнее обновление:** 20.02.2026
**Автор:** Claude Sonnet 4.6 + Разработчик

**Изменения в версии 1.8:**
- **Загрузка скриншота отзыва напрямую из мини-приложения (без бота):**
  - Причина: бот занят Senler'ом (webhook для рассылок) — нельзя принимать входящие сообщения
  - Убраны: `awaitingReviewPhoto` Map, обработчик `bot.on("message:photo")`, `POST /api/request-review`, deeplink `review_*`, `bot.start()`
  - Добавлен: `multer` middleware, `POST /api/upload-review` (multipart/form-data)
  - В мини-приложении: скрытый `<input type="file">`, превью выбранного фото, кнопка "Отправить скриншот" активна только после выбора файла
  - Бот используется только для `bot.api.sendMessage` (исходящие уведомления)
- **Исправлен статус `reviewPending` после перезагрузки:**
  - Раньше: `reviewPending` хранился только локально (сбрасывался при перезагрузке)
  - Теперь: `/api/user` возвращает `reviewPending: true` для заданий с активной `pending` заявкой в БД
  - `mappers.ts`: `isReviewPending` читается из `apiTask.reviewPending` (не из `apiTask.status`)
- **Разрешена повторная отправка после отклонения:**
  - Проверка в `/api/upload-review` блокирует только `pending` и `approved` статусы
  - После отклонения (`rejected`) пользователь снова видит кнопку выбора файла
- **Настроена раздача загруженных файлов через Nginx:**
  - Добавлен `location /uploads/` в конфиг Nginx (`/etc/nginx/sites-enabled/gorodsporta`)
  - Файлы отдаются напрямую без проксирования через Node.js

**Изменения в версии 1.7:**
- **Исправлена автоматическая засчитка задания "Оставь отзыв" (task 11):**
  - Проблема: задание 11 имело `verification_type = 'none'` в БД → бэкенд засчитывал его автоматически
  - Решение: изменить в БД: `UPDATE tasks SET verification_type = 'review' WHERE day_number = 11;`
  - В `/api/complete-task` добавлен явный блок-запрет для `verification_type = 'review'` (возвращает 400)
  - Теперь задание 11 засчитывается ТОЛЬКО через `POST /admin/api/reviews/:id/approve`
- **Карточка задания "Оставь отзыв" в мини-приложении:**
  - Порядок элементов: описание → инструкция → ссылка на Яндекс.Карты → кнопка
  - Кнопка "Выполнить" заменена на "Отправить скриншот"
  - Новое поле `reviewPending` в интерфейсе Task

**Изменения в версии 1.6:**
- **Система верификации отзывов на Яндекс.Картах:**
  - Пользователь отправляет скриншот отзыва боту в Telegram
  - Бот скачивает фото и сохраняет на сервере (`/uploads/reviews/`)
  - Создаётся запись в таблице `review_submissions` со статусом `pending`
  - Новая страница **Reviews** в админке с карточками заявок и превью фото
  - Кнопки "Одобрить" (начисляет монеты + уведомление в TG) и "Отклонить" (с комментарием + уведомление)
  - Фильтрация по статусам: pending / approved / rejected / all
  - Оранжевый бейдж в боковом меню с количеством pending заявок
  - SQL файл для создания таблицы: `bot/create-reviews-table.sql`
- **Управление заданиями в карточке пользователя (UserDetails):**
  - Раздел "Задания" теперь показывает ВСЕ задания (не только выполненные)
  - Кнопка "✓ Засчитать" для ручного зачёта невыполненного задания
  - Кнопка "↩ Отменить" для отмены выполненного задания
  - При зачёте/отмене — автоматически пересчитываются coins и XP
  - Счётчик "Выполнено X из Y" в заголовке раздела
  - Действия доступны только администратору
- **Новые API эндпоинты:**
  - `POST /admin/api/users/:id/toggle-task` — засчитать/отменить задание
  - `GET /admin/api/reviews` — список заявок с фильтром по статусу
  - `GET /admin/api/reviews/count` — счётчик pending заявок
  - `POST /admin/api/reviews/:id/approve` — одобрить отзыв
  - `POST /admin/api/reviews/:id/reject` — отклонить отзыв
  - `POST /api/request-review` — зарегистрировать ожидание фото

**Изменения в версии 1.5:**
- **Унификация проверки кодов на бэкенде (bot/index.js):**
  - Единый порядок проверки во всех трёх блоках: `test_code` → `manual_code` → `qr_code`
  - Блок `app_code`: добавлена проверка `manual_code` (ранее отсутствовала, из-за чего ручные коды не принимались)
  - Блок `qr_or_manual`: убрано ограничение длины кода (ранее был `inputCode.length === 5`)
  - Блок `qr`: добавлена проверка `manual_code`, код теперь всегда приводится к верхнему регистру
  - Удалён мёртвый дублирующий блок `if (false && ...)`
- **Метки блока 3 в мини-приложении (webapp-react):**
  - `TaskCard.tsx`: метка блока 3 исправлена "Лояльность" → "Заминка"
  - `mappers.ts`: зона задания 13 исправлена "Рефералка" → "Онлайн"
- **⚠️ Важно: рассинхронизация dist/ и deploy/ для webapp-react:**
  - Сборка `npm run build` создаёт файлы в `webapp-react/dist/`
  - Nginx раздаёт файлы из папки `deploy/` (в корне проекта)
  - После каждого билда обязательно копировать: `cp -r webapp-react/dist/. deploy/`
  - Затем делать `git add deploy/ && git commit && git push` и `git pull` на сервере

**Изменения в версии 1.4:**
- **Система проверочных кодов:**
  - Реализовано 3 типа кодов для заданий: тестовый, QR-код, ручной код
  - 🔧 Тестовый код (синий) - для внутренней проверки работы задания
  - 📱 QR-код (зелёный) - длинный код, зашитый в QR-коде
  - ⌨️ Ручной код (фиолетовый) - короткий 5-символьный код для ручного ввода
  - Коды редактируются в админке и вступают в силу мгновенно
  - Можно заполнить 1, 2 или все 3 кода - все работает
- **Excel экспорт:**
  - Исправлен экспорт данных анкеты (goals как массив, hasKids как строка)
  - Теперь корректно отображаются "Цели" и "Есть дети" в Excel
- **RBAC (Role-Based Access Control):**
  - Две роли: Администратор (полный доступ) и Сотрудник (только просмотр)
  - Администратор: может редактировать, удалять, менять коды
  - Сотрудник: может просматривать, экспортировать в Excel
  - Роль отображается в правом верхнем углу админки
- **Удаление пользователей:**
  - Исправлена ошибка с foreign key constraint для таблицы referrals
  - Каскадное удаление: user_tasks → purchases → referrals → users
  - Подробное логирование процесса удаления
  - Возврат детальных ошибок для диагностики

**Изменения в версии 1.3:**
- Добавлено автооткрытие блоков на основе прогресса выполнения
- Блоки 2 и 3 автоматически открываются при перезагрузке, если задания выполнены
- Улучшен пользовательский опыт - не нужно повторно нажимать "Продолжить"

**Изменения в версии 1.2:**
- Исправлена ошибка HTTP 500 при редактировании заданий
- Добавлена страница "Рефералы" для отдела продаж
- Добавлен блок "Данные анкеты" в детальной странице пользователя
- Улучшен UX диалогов редактирования (автозаполнение, показ дельты)
- Убрана кнопка "Редактировать" из списка пользователей

**Изменения в версии 1.1:**
- Добавлена функция удаления пользователей
- Обновлена информация об эндпоинтах
