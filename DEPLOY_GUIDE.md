# 🚀 Руководство по деплою Город Спорта

## Проблема: Бесконечная загрузка в миниаппе

### Сделанные исправления:

1. **Добавлен `base: "/"` в vite.config.ts**
   - Гарантирует, что пути к ассетам будут правильными

2. **Добавлено логирование в ключевых местах**
   - TelegramContext теперь логирует инициализацию
   - Index.tsx логирует загрузку данных
   - main.tsx ловит ошибки при инициализации

3. **Добавлена обработка ошибок**
   - Если приложение не загружается, будет показано сообщение об ошибке

4. **Создан debug.html для диагностики**
   - Проверяет окружение, Telegram SDK, API и ассеты

---

## 📋 Шаги для решения проблемы:

### Шаг 1: Диагностика

1. Залейте все файлы из папки `deploy/` на сервер Timeweb
2. Откройте в браузере: `https://gsbot18.ru/debug.html`
3. Проверьте результаты всех тестов

**Что проверяет debug.html:**
- ✅ Загружается ли Telegram WebApp SDK
- ✅ Доступен ли API сервер
- ✅ Загружаются ли файлы JavaScript и CSS

### Шаг 2: Проверка консоли браузера

1. Откройте ваше приложение в Telegram
2. Если есть возможность - откройте DevTools (инструменты разработчика)
3. Посмотрите в консоли логи:
   - `TelegramContext: initializing...`
   - `Telegram WebApp available: true/false`
   - `Index: loadData effect triggered`

### Шаг 3: Проверка конфигурации сервера

На Timeweb убедитесь, что:

1. **Правильная точка входа:**
   - Корневая директория веб-сервера должна указывать на папку с `index.html`

2. **Настройка для SPA (Single Page Application):**

   Создайте файл `.htaccess` в корне (если используется Apache):
   ```apache
   <IfModule mod_rewrite.c>
     RewriteEngine On
     RewriteBase /
     RewriteRule ^index\.html$ - [L]
     RewriteCond %{REQUEST_FILENAME} !-f
     RewriteCond %{REQUEST_FILENAME} !-d
     RewriteRule . /index.html [L]
   </IfModule>
   ```

   Или для Nginx добавьте в конфигурацию:
   ```nginx
   location / {
     try_files $uri $uri/ /index.html;
   }
   ```

3. **Правильные MIME-типы:**
   - `.js` файлы должны отдаваться с `Content-Type: application/javascript`
   - `.css` файлы с `Content-Type: text/css`

---

## 🔍 Возможные причины проблемы:

### 1. Файлы ассетов не найдены (404)
**Симптомы:** В консоли браузера ошибки 404 на `/assets/index-*.js`

**Решение:**
- Убедитесь, что папка `assets/` загружена на сервер
- Проверьте права доступа к файлам (должны быть читаемые)

### 2. Telegram SDK не загружается
**Симптомы:** `Telegram WebApp available: false` в логах

**Решение:**
- Проверьте, что скрипт Telegram загружается: `https://telegram.org/js/telegram-web-app.js`
- Убедитесь, что приложение открывается именно из Telegram

### 3. API недоступен
**Симптомы:** Ошибки сети при запросах к `https://gsbot18.ru/api/...`

**Решение:**
- Проверьте, что бэкенд запущен и доступен
- Проверьте CORS настройки на сервере
- Убедитесь, что firewall не блокирует запросы

### 4. Приложение ждет Telegram ID
**Симптомы:** Застревает на экране загрузки

**Решение:**
- Проверьте логи: `isReady` и `telegramId` в консоли
- Если `telegramId` null - проверьте настройки бота и WebApp URL

---

## 🛠 Как собрать новый билд:

```bash
# Перейти в папку webapp-react
cd webapp-react

# Установить зависимости (если нужно)
npm install

# Собрать production билд
npm run build

# Скопировать в папку deploy
cp -r dist/* ../deploy/
```

---

## 📤 Как задеплоить на Timeweb:

### Вариант 1: Через FTP/SFTP
1. Подключитесь к серверу через FileZilla или другой FTP-клиент
2. Загрузите все файлы из папки `deploy/` в корневую директорию веб-сервера
3. Убедитесь, что загрузились:
   - `index.html`
   - `debug.html`
   - папка `assets/` со всем содержимым
   - `favicon.ico`, `robots.txt`, `placeholder.svg`

### Вариант 2: Через Git (если настроен)
```bash
git add .
git commit -m "fix: resolve infinite loading issue"
git push origin main
```

Затем на сервере:
```bash
git pull origin main
```

### Вариант 3: Через панель Timeweb
1. Войдите в панель управления Timeweb
2. Перейдите в файловый менеджер
3. Загрузите файлы через веб-интерфейс

---

## ✅ Проверка после деплоя:

1. Откройте `https://gsbot18.ru/debug.html` - все тесты должны быть зелёными
2. Откройте приложение в Telegram
3. Проверьте консоль браузера на наличие логов
4. Убедитесь, что приложение загружается

---

## 🆘 Если проблема не решена:

Отправьте скриншоты:
1. Страницы `debug.html`
2. Консоли браузера (F12 → Console)
3. Вкладки Network в DevTools (F12 → Network)

---

## 📝 Структура файлов после деплоя:

```
/корневая_директория_сервера/
├── index.html           (главная страница)
├── debug.html           (диагностическая страница)
├── favicon.ico
├── placeholder.svg
├── robots.txt
├── .htaccess           (создать если используется Apache)
└── assets/
    ├── index-D9_RnF4v.js
    ├── index-D9_RnF4v.js.map
    └── index-wjuNMKg6.css
```

---

## 🔗 Полезные ссылки:

- [Документация Telegram WebApp](https://core.telegram.org/bots/webapps)
- [Vite Documentation](https://vitejs.dev/guide/build.html)
- [Timeweb FAQ](https://timeweb.com/ru/help/)
